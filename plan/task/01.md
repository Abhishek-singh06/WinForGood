# Task 01: Database Schema Migration for Lower-Tier Accounting

## Contexto
The database schema scaffolding created in Phase 5 foundation (`20260920000004_phase5_draws_foundation.sql`) needs support for TA-006 (unclaimed lower-tier funds disposition to charity and remainder tracking).

## Subtareas
- [x] Create `supabase/migrations/20260920000005_phase5_production_engine.sql` adding `unclaimed_amount NUMERIC(12,2) DEFAULT 0.00`, `disposition TEXT DEFAULT NULL`, and `remainder_amount NUMERIC(12,2) DEFAULT 0.00` to `public.draw_tier_allocations`.
- [x] Ensure immutability trigger covers these new columns on published draws.
- [x] Update `docs/migrations.md` to document migration 000005.
