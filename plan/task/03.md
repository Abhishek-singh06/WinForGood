# Task 03: Ticket Mapper & Dynamic Partial Eligibility Resolver

## Contexto
Implement ticket generation according to Decision #1 (BR-047) and eligibility resolution according to TA-002 (Model B: Dynamic Partial Tickets).

## Subtareas
- [x] Create `src/lib/draws/mapper.ts` implementing `IScoreToTicketMapper`:
  - Retains scores as-is (1-45), sorted ascending.
  - Raw duplicates preserved.
  - Empty scores array returns `[]`.
- [x] Create `src/lib/draws/eligibility.ts` implementing `IDrawEligibilityResolver`:
  - Model B: active subscription qualifies any subscriber, having 0 to 5 retained scores.
  - Expose helper to evaluate winning capability by distinct score count.
