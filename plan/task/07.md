# Task 07: Admin & Subscriber Draw UI Enhancement

## Contexto
Update Admin `/admin/draws` and Subscriber `/dashboard/draws` UI to present complete Phase 5 functionality with explicit simulation staging, result inspection, charity disposition breakdown, and subscriber ticket verification.

## Subtareas
- [x] Update `src/components/admin/AdminDrawManager.tsx`:
  - Show draft creation with mode selection (Random / Algorithmic).
  - Show simulated state with complete breakdown: total subscribers, winning numbers, tier allocations, winner counts, prize per winner, jackpot rollover out, and charity disposition ledger for unclaimed lower tiers.
  - Publish button with confirmation modal.
  - Published state showing read-only immutable audit view.
- [x] Update `src/app/dashboard/draws/page.tsx`:
  - Display current subscriber participation state (active subscription check).
  - Display rolling 5 scores and generated ticket.
  - When draw is published, display winning numbers, subscriber matched numbers count, tier won (if any), estimated/allocated prize amount, and verification status.
  - If draw is in draft/simulated, display clear notice that simulation/draft is underway.
